# v1.5.2 Bugfix Round 2 — 跨AI报告验证修复

## TL;DR

> **Quick Summary**: 综合 KIMI-CS 和 Claude-CS 两份 AI 测试报告的交叉验证结果，修复 10 个真实遗留 Bug（P1×3 + P2×7），涵盖状态泄漏、测试覆盖、数据正确性、UX 和样式保留。
>
> **Deliverables**:
> - 修正版 `excel.html`（状态管理、自测覆盖、Sheet名唯一性、数据匹配逻辑、split-rows 样式保留、Time-Slicing、移动端适配）
> - 更新版 `DESIGN.md`
> - 补充测试用例
>
> **Estimated Effort**: Medium（约 2-3 小时）
> **Parallel Execution**: YES - 4 waves
> **Critical Path**: T1 → T3 → T7 → F1-F4

---

## Context

### Original Request
用户提供了两份AI测试报告（KIMI-CS.md 和 claude-cs-test-report-v2.0.md），要求逐条验证并制定修复计划。

### Interview Summary
**Key Discussions**:
- **优先级策略**：先 P1（必须修），再 P2（建议修），P3（可延期）
- **范围**：仅修复经代码验证的真实 Bug，排除假阳
- **版本**：v1.5.2

**Research Findings**:
- R1: `onSmartMergeModeChange` IIFE 闭包问题 — L1370-1371 `onchange` 无法触及 L6426 函数定义
- R2: `resetTool()` 漏清 6 个全局变量 — L1636-1645 声明，L2971-3000 未重置
- R3: 自测 modes 数组仅 5/9 — L7404
- R4: 模式B Sheet名截断没用 `getUniqueSheetName` — L6712-6713
- R5: data-join 空匹配 throw 而非输出空文件 — L7226
- R6: `getEffectiveProcessingMode` 缺 2 个模式 — L1730-1738
- R7: split-rows `aoa_to_sheet` 丢样式 — L5404/L5463
- R8: 模式B 同步 `forEach` 无 Time-Slicing — L6686
- R9: 移动端 9 按钮溢出 — L1009-1010
- R10: 合并单元格偏移 `srpHeaderRows + srpStart - srpHeaderRows` 可化简 — L5472

### Metis Review
**Identified Gaps** (auto-resolved):
- ModeB forEach sync: 归属于 ModeB 改造（T8），与 T5 并行
- onSmartMergeModeChange 影响范围：仅描述文字更新，功能无影响，归 P2
- 不需要修的：P2-8 实际无影响（summary-merge/data-join 当前无处理模式 UI）、A1 文档（功能正确）、A2 架构重构（长期）

---

## Work Objectives

### Core Objective
修复 10 个经交叉验证的真实 Bug，提升状态管理健壮性、测试覆盖完整性和功能正确性。

### Concrete Deliverables
- 修复后验收：`excel.html` 通过全部 60 个 Playwright 测试 + 164 个内置自测
- 状态无残留：连续两次不同模式操作，统计信息各自独立

### Must Have
- resetTool 清零所有 6 个遗漏变量
- 自测覆盖所有 9 个模式按钮
- 模式B Sheet 名使用 `getUniqueSheetName` 保障唯一性
- `onSmartMergeModeChange` 绑定改为 `addEventListener`
- data-join 空匹配输出空文件而非抛异常

### Must NOT Have (Guardrails)
- 不触及已有工作模式的核心合并/拆分算法逻辑（仅修复边界条件）
- 不引入新的外部依赖
- 不更改 HTML 结构（CSS 类名、ID，除 addEventListener 替换 onchange 外）

---

## Verification Strategy (MANDATORY)

> **ZERO HUMAN INTERVENTION** — ALL verification via automated test suite.

### Test Decision
- **Infrastructure exists**: YES (Playwright + built-in self-test)
- **Automated tests**: None (existing tests suffice + new tests per task)
- **All verification**: Agent-executed QA via Playwright + bash

### QA Policy
Every task verified by running existing test suite + targeted functional check.

---

## Execution Strategy

### Parallel Execution Waves

```
Wave 1 (Start Immediately - simple fixes, MAX PARALLEL):
├── T1: resetTool + 6 globals reset [quick]
├── T2: Self-test modes array → 9 modes [quick]
├── T3: getEffectiveProcessingMode + summary-merge/data-join [quick]
└── T4: split-rows merge cell expression simplify [quick]

Wave 2 (After Wave 1 - medium complexity):
├── T5: onSmartMergeModeChange addEventListener [unspecified-high]
├── T6: ModeB sheet name getUniqueSheetName + truncation fix [deep]
├── T7: data-join empty match → output empty file [deep]
└── T8: ModeB forEach → processWithYielding [unspecified-high]

Wave 3 (After Wave 2 - deep changes):
├── T9: split-rows style preservation (copyWorksheetByDeletion) [deep]
└── T10: Mobile responsive — mode button grouping [visual-engineering]

Wave FINAL (After ALL tasks — 4 parallel reviews, then user okay):
├── F1: Plan compliance audit (oracle)
├── F2: Code quality review (unspecified-high)
├── F3: Full test suite run (Playwright 60 + self-test 164)
└── F4: Scope fidelity check (deep)

Critical Path: T1 → T5 → T7 → T9 → F1-F4 → user okay
```

### Dependency Matrix

- **T1**: T5, T6, T7, T8, T9, T10 — T2, T3, T4
- **T2**: T6 — T9 — T3
- **T5**: T7, T8 — T9 - 2
- **T7**: T9 - 2
- **T9**: F1-F4 - 3
- **T10**: F1-F4 - 3

---

## TODOs

- [ ] 1. **resetTool: 补清零 6 个全局变量 — P1-9**

  **What to do**:
  在 `resetTool()` 函数（L2971-3000）末尾 `goToStep(1)` 之前，追加以下变量清零：
  ```javascript
  splitReport = null;
  selectedSheetForColumn = null;
  originalFileType = 'xlsx';
  isXlsFile = false;
  lastMergeStatistics = null;
  lastSkippedSheets = [];
  ```
  - 这些变量声明在 L1636-1645 和 L8835-8836
  - 注意：`originalFileType` 默认值 = `'xlsx'`，`isXlsFile` 默认值 = `false` — 与初始化一致
  - `lastMergeStatistics` 声明为 `null`，重置为 `null`
  - `lastSkippedSheets` 声明为 `[]`，重置为 `[]`

  **Must NOT do**:
  - 不修改其他已有重置逻辑
  - 不改变现有变量的默认值语义

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: 单文件单函数追加 6 行代码，无需复杂逻辑
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with T2, T3, T4)
  - **Blocks**: T5, T6, T7, T8, T9, T10
  - **Blocked By**: None

  **References**:
  - `excel.html:2971-3000` — 当前 resetTool() 函数（编辑目标）
  - `excel.html:1636-1637,1644-1645` — `selectedSheetForColumn`、`splitReport`、`originalFileType`、`isXlsFile` 声明
  - `excel.html:8835-8836` — `lastMergeStatistics`、`lastSkippedSheets` 声明

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: 连续两次不同模式操作后状态应独立
    Tool: Playwright
    Preconditions: 正常浏览器环境
    Steps:
      1. 进入 split-column 模式，上传测试文件，完成一次拆分
      2. 切换到 merge-sheet 模式（触发 switchMode → resetTool）
      3. 检查 splitReport 是否为 null（通过控制台执行表达式）
    Expected Result: splitReport === null
    Evidence: .omo/evidence/task-1-state-clean.txt

  Scenario: 变量清零后首次操作用户界面正常
    Tool: Playwright
    Steps:
      1. reload 页面 → 上传文件 → 选择拆分模式 → 完成操作
      2. 确认 resultSummary 正确显示
    Expected Result: 操作正常完成，无异常统计信息残留
    Evidence: .omo/evidence/task-1-operation-clean.txt
  ```

  **Evidence to Capture**:
  - [ ] task-1-state-clean.txt
  - [ ] task-1-operation-clean.txt

  **Commit**: YES (group with all Wave 1)
  - Message: `v1.5.2: cross-AI-test-report bugfix round 2`
  - Files: `excel.html`

- [ ] 2. **自测 modes 数组扩展为 9 个模式 — P1-10**

  **What to do**:
  在 `excel.html` L7404，将：
  ```javascript
  const modes = ['split-sheet', 'split-column', 'split-column-vertical', 'merge-file', 'merge-sheet'];
  ```
  修改为：
  ```javascript
  const modes = ['split-sheet', 'split-column', 'split-column-vertical', 'split-rows', 'merge-file', 'merge-sheet', 'smart-merge', 'summary-merge', 'data-join'];
  ```
  - 保持数组顺序与 HTML 中按钮排列一致
  - `split-rows` 放在 `split-column-vertical` 之后（是第 4 个拆分模式）
  - 三个高级模式（smart-merge、summary-merge、data-join）放在 `merge-sheet` 之后

  **Must NOT do**:
  - 不修改其他自测逻辑
  - 不改变按钮的 `data-mode` 属性

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: 单行数组修改，零风险
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with T1, T3, T4)
  - **Blocks**: None
  - **Blocked By**: None

  **References**:
  - `excel.html:7404` — 当前 5 模式数组
  - `excel.html:1081-1091` — 9 个实际模式按钮的 HTML

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: 自测 T04 覆盖全部 9 个模式
    Tool: Playwright
    Preconditions: 浏览器加载页面 + ?selftest=1 触发
    Steps:
      1. 导航到页面 URL?selftest=1
      2. 等待自测完成
      3. 检查控制台输出是否包含所有 9 个模式按钮的 T04 通过消息
    Expected Result: 9 个 T04 测试全部 pass
    Evidence: .omo/evidence/task-2-self-test-9-modes.txt
  ```

  **Evidence to Capture**:
  - [ ] task-2-self-test-9-modes.txt

  **Commit**: YES (group with Wave 1)

- [ ] 3. **getEffectiveProcessingMode 补全映射 — P2-8**

  **What to do**:
  在 `excel.html` L1730-1738，`idMap` 对象中追加两个映射：
  ```javascript
  'summary-merge': null,
  'data-join': null
  ```
  - 这两个模式当前无处理模式 UI 选择器，所以值设为 `null`
  - 但显式声明映射确保：
    1. 函数一致性 — 所有已知模式都能在 idMap 中找到
    2. 未来添加 UI 时不会遗漏
    3. 审查者能清晰看到哪些模式有/无处理模式覆盖

  **Must NOT do**:
  - 不修改函数返回值逻辑
  - 不添加实际的处理模式 UI（仅映射）

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: 追加 2 行对象属性，零风险
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with T1, T2, T4)
  - **Blocks**: None
  - **Blocked By**: None

  **References**:
  - `excel.html:1729-1745` — `getEffectiveProcessingMode` 函数

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: 映射补全后函数返回正常
    Tool: Playwright (page.evaluate)
    Preconditions: 浏览器加载页面
    Steps:
      1. page.evaluate(() => { /* 调用内部函数检查 */ })
      2. 对 summary-merge 和 data-join 传入无覆盖模式时返回默认值
    Expected Result: 不抛出异常
    Evidence: .omo/evidence/task-3-mapping-ok.txt
  ```

  **Evidence to Capture**:
  - [ ] task-3-mapping-ok.txt

  **Commit**: YES (group with Wave 1)

- [ ] 4. **split-rows 合并单元格偏移表达式化简 — P2-10**

  **What to do**:
  在 `excel.html` L5472，将：
  ```javascript
  if (adj.s.r >= srpHeaderRows) adj.s.r -= srpHeaderRows + srpStart - srpHeaderRows;
  ```
  修改为：
  ```javascript
  if (adj.s.r >= srpHeaderRows) adj.s.r -= srpStart;
  ```
  同理 L5473（如果存在同样的冗余表达式）。

  - 简化原因：`srpHeaderRows + srpStart - srpHeaderRows` = `srpStart`，恒成立
  - 这是纯可读性改善，不影响逻辑

  **Must NOT do**:
  - 不改变实际的行偏移计算结果

  **Recommended Agent Profile**:
  - **Category**: `quick`
    - Reason: 1 行数学化简，零风险
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: YES
  - **Parallel Group**: Wave 1 (with T1, T2, T3)
  - **Blocks**: None
  - **Blocked By**: None

  **References**:
  - `excel.html:5466-5476` — 合并单元格处理代码块

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: 表达式化简后合并单元格偏移不变
    Tool: 逻辑验证（已被现有测试覆盖）
    Steps:
      1. 运行 split-rows 现有测试（specs/08-v2-features.spec.js SR-01/SR-02/SR-03）
      2. 所有测试通过即验证成功
    Expected Result: SR-01/SR-02/SR-03 全部 pass
    Evidence: .omo/evidence/task-4-expression-simplify.txt
  ```

  **Evidence to Capture**:
  - [ ] task-4-expression-simplify.txt

  **Commit**: YES (group with Wave 1)

- [ ] 5. **onSmartMergeModeChange: HTML onchange → addEventListener — P0-1**

  **What to do**:
  **第一步**：移除 HTML 中的 `onchange` 属性
  在 `excel.html` L1370-1371：
  ```html
  <label><input type="radio" name="smartMergeMode" value="modeA" checked onchange="onSmartMergeModeChange()"> 模式A：全量纵向追加</label>
  <label><input type="radio" name="smartMergeMode" value="modeB" onchange="onSmartMergeModeChange()"> 模式B：按名称归类</label>
  ```
  改为：
  ```html
  <label><input type="radio" name="smartMergeMode" value="modeA" checked> 模式A：全量纵向追加</label>
  <label><input type="radio" name="smartMergeMode" value="modeB"> 模式B：按名称归类</label>
  ```

  **第二步**：添加 `addEventListener` 绑定
  在 `switchMode` 函数末尾（或 IIFE 初始化代码中），添加：
  ```javascript
  document.querySelectorAll('input[name="smartMergeMode"]').forEach(function(el) {
    el.addEventListener('change', onSmartMergeModeChange);
  });
  ```
  建议位置：与 L3001 附近其他 UI 初始化代码放在一起，或在 `setupUI` 类似的初始化段中。

  **Must NOT do**:
  - 不修改 `onSmartMergeModeChange` 函数体
  - 不在 HTML 中新增内联脚本

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: 涉及 HTML 结构改动 + JS 事件绑定，需要确保绑定时机正确
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: NO (depends on T1 for clean state)
  - **Parallel Group**: Wave 2 (with T6, T7, T8)
  - **Blocks**: None
  - **Blocked By**: T1

  **References**:
  - `excel.html:1370-1371` — HTML onchange 属性（待移除）
  - `excel.html:6426` — `onSmartMergeModeChange` 函数定义
  - `excel.html:3002-3033` — `switchMode` 函数（绑定初始化位置参考）

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: 切换 smart-merge 模式A/B 描述文字更新
    Tool: Playwright
    Preconditions: 页面加载完成
    Steps:
      1. 点击 smart-merge 模式按钮
      2. 点击模式B 单选按钮
      3. 检查模式描述文本是否更新为模式B 的描述
    Expected Result: 描述文字随模式切换更新
    Evidence: .omo/evidence/task-5-smart-mode-switch.txt

  Scenario: 控制台无 ReferenceError
    Tool: Playwright
    Steps:
      1. 监听 page.on('pageerror')
      2. 重复以上操作
    Expected Result: 控制台无任何错误
    Evidence: .omo/evidence/task-5-no-console-error.txt
  ```

  **Evidence to Capture**:
  - [ ] task-5-smart-mode-switch.txt
  - [ ] task-5-no-console-error.txt

  **Commit**: YES (group with Wave 2)

- [ ] 6. **模式B Sheet名使用 getUniqueSheetName 防止重名 — P1-11**

  **What to do**:
  在 `excel.html` L6711-6714，将：
  ```javascript
  if (gMerged) {
      // Sheet名超31字符截断
      var finalName = gName.length > 28 ? gName.substring(0, 28) + '...' : gName;
      XLSX.utils.book_append_sheet(smBwb, gMerged, finalName);
  }
  ```
  修改为：
  ```javascript
  if (gMerged) {
      // 使用 getUniqueSheetName 确保唯一性，同时处理截断
      var safeName = gName.length > 28 ? gName.substring(0, 28) + '...' : gName;
      var finalName = getUniqueSheetName(smBwb.SheetNames, safeName, smBg);
      XLSX.utils.book_append_sheet(smBwb, gMerged, finalName);
  }
  ```

  **为什么不直接用 getUniqueSheetName 替代截断逻辑？**
  - `getUniqueSheetName` 函数（L3408）会追加 `(N)` 后缀处理重名
  - 但 Excel Sheet 名上限 31 字符，截断逻辑仍需保留
  - 方案：先截断，再用 `getUniqueSheetName` 去重（优先于截断前使用，因为即使不截断也可能重名）

  **边缘情况**：
  - `gName` 是 `normalizeSheetNameForGroup` 的输出（小写、去特殊字符），已经过归一化
  - 两个不同原始名归一化后相同的场景：`getUniqueSheetName` 处理后 `"销售数据"` → `"销售数据(2)"`
  - 原始名信息已在 `gName` 中丢失 — 这是已存在的设计限制，本次不改（涉及模式B 底层结构变更）

  **Must NOT do**:
  - 不改变 `getUniqueSheetName` 函数签名或行为
  - 不修改 `normalizeSheetNameForGroup`

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: 需要理解 getUniqueSheetName 的调用契约和边界情况，确保正确集成
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 2 (with T5, T7, T8)
  - **Blocks**: None
  - **Blocked By**: T1

  **References**:
  - `excel.html:6712-6714` — 当前截断逻辑
  - `excel.html:3408-3425` — `getUniqueSheetName` 函数定义
  - `excel.html:6089` — merge-sheet 中使用 getUniqueSheetName 的同款调用参考
  - `excel.html:6625` — `normalizeSheetNameForGroup` 归一化逻辑

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: 模式B 重名 Sheet 自动去重
    Tool: Playwright
    Preconditions: 准备 2 个 Excel 文件，各有 1 个同名的 "销售" Sheet
    Steps:
      1. 进入 smart-merge 模式 → 上传 2 个文件 → 选所有 Sheet
      2. 选择模式B → 执行合并 → 下载
      3. 打开合并文件，检查 Sheet 列表
    Expected Result: 有 "销售" 和 "销售(2)" 两个 Sheet，无重复名
    Evidence: .omo/evidence/task-6-unique-sheet-names.txt

  Scenario: 正常合并不引入回归
    Tool: Playwright
    Steps:
      1. 运行现有 smart-merge 测试（SM-B01）
    Expected Result: SM-B01 pass
    Evidence: .omo/evidence/task-6-no-regression.txt
  ```

  **Evidence to Capture**:
  - [ ] task-6-unique-sheet-names.txt
  - [ ] task-6-no-regression.txt

  **Commit**: YES (group with Wave 2)

- [ ] 7. **data-join 空匹配输出空文件而非抛异常 — P1-12**

  **What to do**:
  在 `excel.html` L7226，将：
  ```javascript
  if (resultRows.length === 0) throw new Error('没有匹配的数据行');
  ```
  修改为输出空结果文件：
  ```javascript
  if (resultRows.length === 0) {
      // 空匹配是合法结果，输出仅含表头的空文件（用户可确认确实无匹配）
      var emptyWs = XLSX.utils.aoa_to_sheet([resultHeader]);
      if (resultHeader.length > 0) emptyWs['!cols'] = resultHeader.map(() => ({ wch: 14 }));
      var emptyWb = XLSX.utils.book_new();
      XLSX.utils.book_append_sheet(emptyWb, emptyWs, '数据匹配结果(空)');
      injectFullCalcOnLoad(emptyWb);
      var emptyBuf = XLSX.write(emptyWb, getWriteOptions());
      emptyBuf = await injectFullCalcOnLoadIntoBuffer(emptyBuf);
      mergedFileBlob = new Blob([emptyBuf], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
      mergedFileName = getMergeOutputFileName();
      lastMergeStatistics = {
          sourceFileCount: uploadedFiles.length,
          totalSheets: 2,
          mergedRows: 0,
          note: joinType + ' join (0 matched)'
      };
      lastSkippedSheets = [];
      loadingText.textContent = '完成！（0 行匹配）';
      progressBarFill.style.width = '100%';
      await new Promise(resolve => setTimeout(resolve, UI_YIELD_DELAY_LARGE));
      loading.style.display = 'none';
      document.getElementById('totalFiles').textContent = '1';
      document.getElementById('totalSize').textContent = formatFileSize(mergedFileBlob.size);
      csShow(document.getElementById('resultSummary'));
      csShow(document.getElementById('downloadBtn'), 'cs-inline-flex');
      if (lastMergeStatistics) showMergeStatistics(lastMergeStatistics);
      showSkippedSheetsInfo(lastSkippedSheets);
      isProcessing = false;
      showToast('数据匹配成功（0 行匹配）', 'success');
      return;
  }
  ```
  - 注意：需要使用该作用域中已有的变量（`resultHeader`、`uploadedFiles`、`joinType` 等）
  - 这段代码放在原位置，与后续的正常输出流程完全并行 — 一个 `return` 终止当前路径
  - `loadingText` 变量已在函数上部定义（需确认作用域可达）

  **Must NOT do**:
  - 不改变正常（有匹配）的执行路径
  - 不改变 `resultHeader` 的构建逻辑

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: 需要理解整个 data-join 函数的控制流、变量作用域和显示逻辑
    - 注意：`loadingText`、`progressBarFill`、`loading`、`csShow`、`showToast`、`showMergeStatistics`、`showSkippedSheetsInfo`、`formatFileSize` 均在该函数作用域内（函数内闭包或 IIFE 全局），应可直接访问
    - 但 `isProcessing` 是全局变量（L1650），`getMergeOutputFileName()` 在 IIFE 内定义
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 2 (with T5, T6, T8)
  - **Blocks**: T9
  - **Blocked By**: T1

  **References**:
  - `excel.html:7226` — 当前抛出异常的位置
  - `excel.html:7091-7260` — 整个 `performDataJoin` 函数体（理解控制流）
  - `excel.html:7228-7254` — 正常输出流程（复制这个模式的 success handler）
  - `excel.html:7243-7249` — 正常 lastMergeStatistics 结构

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: inner join 无匹配输出空文件
    Tool: Playwright
    Preconditions: 测试数据 a 和 b 的关键列无重叠
    Steps:
      1. 进入 data-join 模式 → 上传 a 和 b
      2. 选择 inner join → 执行
      3. 应显示成功提示 "0 行匹配"
      4. 下载文件
    Expected Result: 下载成功，文件仅含表头，无数据行
    Evidence: .omo/evidence/task-7-empty-join.txt

  Scenario: 正常匹配路径无回归
    Tool: Playwright
    Steps:
      1. 运行现有 DJ-01、DJ-02、DJ-03 测试
    Expected Result: 全部 pass
    Evidence: .omo/evidence/task-7-normal-join.txt
  ```

  **Evidence to Capture**:
  - [ ] task-7-empty-join.txt
  - [ ] task-7-normal-join.txt

  **Commit**: YES (group with Wave 2)

- [ ] 8. **模式B 数据追加 forEach → processWithYielding — P2-6**

  **What to do**:
  在 `excel.html` L6686，将：
  ```javascript
  gDataRows.forEach(function(rowItem, localIdx) {
      var rowArr = rowItem || [];
      var absRow = gStartRow + localIdx;
      for (var c = 0; c < gMaxCols; c++) {
          var val = c < rowArr.length ? rowArr[c] : '';
          var addr = XLSX.utils.encode_cell({ r: absRow, c: c });
          if (typeof val === 'number') {
              gMerged[addr] = { t: 'n', v: val };
          } else if (val !== undefined && val !== null && val !== '') {
              gMerged[addr] = { t: 's', v: String(val) };
          }
      }
      if (addSourceColumn) {
          gMerged[XLSX.utils.encode_cell({ r: absRow, c: gSrcCol })] = { t: 's', v: gSrc2 };
      }
  });
  ```
  改为使用 `processWithYieldingAsync`（注意是异步版，因为不关心进度回调）：
  ```javascript
  await processWithYieldingAsync(gDataRows, function(rowItem, localIdx) {
      var rowArr = rowItem || [];
      var absRow = gStartRow + localIdx;
      for (var c = 0; c < gMaxCols; c++) {
          var val = c < rowArr.length ? rowArr[c] : '';
          var addr = XLSX.utils.encode_cell({ r: absRow, c: c });
          if (typeof val === 'number') {
              gMerged[addr] = { t: 'n', v: val };
          } else if (val !== undefined && val !== null && val !== '') {
              gMerged[addr] = { t: 's', v: String(val) };
          }
      }
      if (addSourceColumn) {
          gMerged[XLSX.utils.encode_cell({ r: absRow, c: gSrcCol })] = { t: 's', v: gSrc2 };
      }
  }, null, { chunkSize: CHUNK_SIZE_ROW });
  ```

  **为什么改成 processWithYieldingAsync 而不是 processWithYielding？**
  - `processWithYielding` 第一个参数是带索引的数组对象，`processWithYieldingAsync` 是纯数组版
  - `gDataRows` 是 flat array，不需要索引映射
  - 传递 `null` 作为 onProgress 回调（已有 `smBTotalSheets` 进度条设在外部循环）
  - `CHUNK_SIZE_ROW` 已在代码中使用（L5831）

  **Must NOT do**:
  - 不改变 `gDataRows` 的内容或顺序
  - 不影响 `gNextRow` 和 `gMerged['!ref']` 的计算（已经在 forEach 之后）

  **Recommended Agent Profile**:
  - **Category**: `unspecified-high`
    - Reason: 需确认 processWithYieldingAsync 签名 + 回调函数内闭包变量是否全部正确闭包
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 2 (with T5, T6, T7)
  - **Blocks**: None
  - **Blocked By**: T1

  **References**:
  - `excel.html:6686-6701` — 当前同步 forEach
  - `excel.html:1683` — `processWithYielding` 函数签名
  - `excel.html:1704` — `processWithYieldingAsync` 函数签名
  - `excel.html:6531` — 模式A 使用 processWithYielding 的参考实现

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: 模式B 合并大文件不假死
    Tool: Playwright
    Preconditions: 准备 2 个多 Sheet 大文件（~500 行每个）
    Steps:
      1. smart-merge → 选择模式B → 上传 + 执行
      2. 观察进度条持续更新
    Expected Result: 合并成功，页面无假死
    Evidence: .omo/evidence/task-8-modeb-responsive.txt

  Scenario: 模式B 基本功能无回归
    Tool: Playwright
    Steps:
      1. 运行 SM-B01 测试
    Expected Result: pass
    Evidence: .omo/evidence/task-8-modeb-regression.txt
  ```

  **Evidence to Capture**:
  - [ ] task-8-modeb-responsive.txt
  - [ ] task-8-modeb-regression.txt

  **Commit**: YES (group with Wave 2)

- [ ] 9. **split-rows 保留格式模式使用 copyWorksheetByDeletion — P2-5**

  **What to do**:
  在 split-rows 的 ZIP 输出路径（L5462-5488）和单文件输出路径（L5398-5426）中，**当处理模式为 `'full'`（保留格式）时**，将 `aoa_to_sheet` 替换为 `copyWorksheetByDeletion` 实现样式保留。

  **详细步骤**：

  1. **在 split-rows 处理函数顶部获取有效处理模式**：
     ```javascript
     var srpEffectiveMode = getEffectiveProcessingMode(currentMode);
     ```

  2. **ZIP 路径（L5462-5488）**：
     当前代码：
     ```javascript
     var srpNewWb = XLSX.utils.book_new();
     var srpNewWs = XLSX.utils.aoa_to_sheet(srpChunkRows);
     ```
     改为（保留格式时）：
     ```javascript
     var srpNewWb = XLSX.utils.book_new();
     var srpNewWs;
     if (srpEffectiveMode === 'full') {
         // 保留格式：使用行删除策略，复制原工作表后删除无关行
         var allRowIndices = [];
         for (var ri = 0; ri < srpTotal; ri++) allRowIndices.push(ri);
         var keepIndices = [];
         // 保留表头行
         for (var hi = 0; hi < srpHeaderRows; hi++) keepIndices.push(hi);
         // 保留当前分片的数据行
         for (var di = srpStart; di < srpEnd; di++) keepIndices.push(srpHeaderRows + di);
         // 标记要删除的行（不在 keepIndices 中的）
         var deleteIndices = allRowIndices.filter(function(idx) { return keepIndices.indexOf(idx) === -1; });
         srpNewWs = await copyWorksheetByDeletion(srpWs, deleteIndices, { deleteRows: true });
         // 清空临时添加的行索引
     } else {
         srpNewWs = XLSX.utils.aoa_to_sheet(srpChunkRows);
     }
     ```
     **注意**：`copyWorksheetByDeletion` 的第二个参数是 `rowsToKeep` 还是 `rowsToDelete` — 需要查看函数签名（L2253）。根据注释 `copyWorksheetByDeletion(worksheet, rowsToKeep, options)`，第二个参数是要保留的行索引数组。所以逻辑应该反过来。

     **修正方案**：
     ```javascript
     var srpNewWb = XLSX.utils.book_new();
     var srpNewWs;
     if (srpEffectiveMode === 'full') {
         // 保留格式：copyWorksheetByDeletion 保留需要的行
         var keepRows = [];
         for (var hi = 0; hi < srpHeaderRows; hi++) keepRows.push(hi);
         for (var di = srpStart + srpHeaderRows; di < srpEnd + srpHeaderRows; di++) keepRows.push(di);
         srpNewWs = await copyWorksheetByDeletion(srpWs, keepRows, undefined, undefined);
     } else {
         srpNewWs = XLSX.utils.aoa_to_sheet(srpChunkRows);
     }
     ```

  3. **单文件路径（L5398-5426）** — 同理改造

  4. **验证**：`copyWorksheetByDeletion` 是异步的（返回 Promise），需要 `await`

  **Must NOT do**:
  - 不改变 `getWriteOptions()` 的调用（已基于处理模式做了配置）
  - 不删除原有 `aoa_to_sheet` 路径（数据模式仍需要用）
  - 不影响 `getEffectiveProcessingMode` 返回值的语义
  - 确保 `copyWorksheetByDeletion` 调用正确闭包了工作簿变量

  **Recommended Agent Profile**:
  - **Category**: `deep`
    - Reason: 需要深入理解 copyWorksheetByDeletion 的签名（rowsToKeep 语义）、split-rows 的两种输出路径、以及行索引的精确计算。风险较高 — 行索引偏差 1 会导致数据错位
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 3
  - **Blocks**: None
  - **Blocked By**: T1, T5

  **References**:
  - `excel.html:2253` — `copyWorksheetByDeletion` 函数签名
  - `excel.html:5398-5426` — split-rows 单文件输出路径
  - `excel.html:5462-5488` — split-rows ZIP 输出路径
  - `excel.html:5895-5901` — split-column 中使用 `copyWorksheetWithFilteredRows` 的参考实现
  - `excel.html:1729-1745` — `getEffectiveProcessingMode`
  - `excel.html:1327-1331` — split-rows 处理模式选择器 HTML

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: split-rows 保留格式模式保留单元格样式
    Tool: Playwright + 文件解析
    Preconditions: 准备一个有背景色/粗体/合并单元格的 Excel
    Steps:
      1. 上传 → split-rows → 保留格式模式 → 执行 → 下载
      2. 用 XLSX.js 打开生成的第一个文件
      3. 检查单元格样式（字体、背景色、合并单元格）是否保留
    Expected Result: 样式与原始文件一致
    Evidence: .omo/evidence/task-9-style-preserved.txt

  Scenario: split-rows 仅数据模式工作正常
    Tool: Playwright
    Steps:
      1. 上传 → split-rows → 仅数据模式 → 执行 → 下载
      2. 检查文件内容正确
    Expected Result: 数据正确，无异常
    Evidence: .omo/evidence/task-9-data-mode.txt

  Scenario: 现有 split-rows 测试无回归
    Tool: Playwright
    Steps:
      1. 运行 SR-01、SR-02、SR-03
    Expected Result: 全部 pass
    Evidence: .omo/evidence/task-9-regression.txt
  ```

  **Evidence to Capture**:
  - [ ] task-9-style-preserved.txt
  - [ ] task-9-data-mode.txt
  - [ ] task-9-regression.txt

  **Commit**: YES (group with Wave 3)

- [ ] 10. **移动端 9 模式按钮溢出修复 — P2-9**

  **What to do**:
  在 `excel.html` CSS 媒体查询段（L1009-1010），改进移动端模式按钮布局。

  当前：
  ```css
  @media (max-width: 768px) {
      ...
      .mode-selector {
          flex-direction: column;
      }
      ...
  }
  ```
  改为 3x3 网格布局：
  ```css
  @media (max-width: 768px) {
      ...
      .mode-selector {
          display: grid;
          grid-template-columns: repeat(3, 1fr);
          gap: 6px;
          padding: 8px;
      }
      .mode-btn {
          font-size: 0.8em;
          padding: 6px 4px;
          white-space: normal;
          text-align: center;
          min-height: 44px; /* iOS 触摸目标 */
      }
      ...
  }
  ```
  同时在 PC 端确保 `display: flex` 不受影响（在非媒体查询区块中保留原有 `.mode-selector` 样式）。

  **为什么不使用折叠面板？** 折叠面板需要 JS 逻辑和 DOM 结构变更，3x3 网格纯 CSS 改动，风险最低。

  **Must NOT do**:
  - 不修改 HTML 结构（`.mode-btn` 的 class/ID）
  - 不影响 768px 以上的桌面端布局
  - 不改动其他移动端 CSS

  **Recommended Agent Profile**:
  - **Category**: `visual-engineering`
    - Reason: CSS 布局调整，需要确保视觉一致性
  - **Skills**: none needed

  **Parallelization**:
  - **Can Run In Parallel**: NO
  - **Parallel Group**: Wave 3
  - **Blocks**: None
  - **Blocked By**: T1

  **References**:
  - `excel.html:1009-1010` — 现有移动端 mode-selector CSS
  - `excel.html:1081-1091` — 9 个按钮的 HTML
  - `excel.html:110-120` — PC 端 .mode-selector 样式

  **Acceptance Criteria**:

  **QA Scenarios (MANDATORY)**:
  ```
  Scenario: 375px 视口下 9 按钮 3x3 排列
    Tool: Playwright
    Preconditions: 设置 viewport 375x812
    Steps:
      1. page.setViewportSize({ width: 375, height: 812 })
      2. 截图
      3. 检查 9 个按钮是否可见且无溢出
    Expected Result: 9 个按钮以 3x3 网格排列，全部可见，无需横向滚动
    Evidence: .omo/evidence/task-10-mobile.png

  Scenario: 桌面端布局不受影响
    Tool: Playwright
    Steps:
      1. page.setViewportSize({ width: 1920, height: 1080 })
      2. 截图
    Expected Result: 按钮横向排列，与之前一致
    Evidence: .omo/evidence/task-10-desktop.png
  ```

  **Evidence to Capture**:
  - [ ] task-10-mobile.png
  - [ ] task-10-desktop.png

  **Commit**: YES (group with Wave 3)

---

## Final Verification Wave (MANDATORY — after ALL implementation tasks)

- [ ] F1. **Plan Compliance Audit** — `oracle`
  Read the plan end-to-end. For each "Must Have": verify implementation exists (grep, run test). For each "Must NOT Have": search codebase for forbidden patterns. Check all 10 issues from the plan are addressed.
  Output: `Must Have [N/N] | Must NOT Have [N/N] | Tasks [N/N] | VERDICT: APPROVE/REJECT`

- [ ] F2. **Code Quality Review** — `unspecified-high`
  Review all changed areas for: console.log in prod, commented-out code, unused variables, `as any`/`@ts-ignore` in JS. Check no regressions introduced.
  Output: `Quality [PASS/REVIEW] | VERDICT`

- [ ] F3. **Full Test Suite Run** — `unspecified-high` (+ `playwright` skill)
  Run `npx playwright test --reporter=list` in `test/` directory. All 60 must pass. Run `npx playwright test specs/00-selftest.spec.js` to verify 164/164 self-tests pass.
  Output: `Playwright [N/N pass] | Self-test [N/N pass] | VERDICT`

- [ ] F4. **Scope Fidelity Check** — `deep`
  For each task: read "What to do", read diff. Verify 1:1 — everything in scope was built, nothing beyond scope was built. Check "Must NOT do" compliance. Detect cross-task contamination.
  Output: `Tasks [N/N compliant] | Contamination [CLEAN/N issues] | VERDICT`

---

## Commit Strategy

- Commit message: `v1.5.2: cross-AI-test-report bugfix round 2`
- Files: `excel.html`
- Pre-commit: full test suite

---

## Success Criteria

### Verification Commands
```bash
cd test && npx playwright test --reporter=list
cd test && npx playwright test specs/00-selftest.spec.js
```

### Final Checklist
- [ ] All 10 issues fixed and verified
- [ ] Playwright 60/60 pass
- [ ] Self-test 164/164 pass
- [ ] No lint/console errors
